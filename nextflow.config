executor {
    name = 'slurm'
}
  
process {
    conda = 'Histone-ChIP-seq-analysis/environment.yml'
    queue = 'irbio01'    // Name of the partition used for processing, change accordingly
    cpus = 16
    clusterOptions = '--job-name=chipseq --nodes=1'
}

executor {
    name = 'slurm'
}
